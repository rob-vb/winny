---
phase: 05-notifications-settings
plan: "03"
type: execute
wave: 3
depends_on:
  - "05-02"
files_modified:
  - src/components/settings/EditableNameRow.tsx
  - app/(tabs)/settings.tsx
autonomous: true
requirements:
  - SET-02

must_haves:
  truths:
    - "User can tap the 'Your Name' row and type their display name in place"
    - "Name saves automatically on blur or Return key press"
    - "If no name is set, the row shows 'Add your name' placeholder"
    - "After saving, the row returns to view mode showing the saved name"
    - "Profile section is wired in settings.tsx with EditableNameRow fully functional"
  artifacts:
    - path: "src/components/settings/EditableNameRow.tsx"
      provides: "Inline-edit row: view mode (label + value) → edit mode (TextInput) on tap; save on blur/return; Reanimated 200ms fade"
    - path: "app/(tabs)/settings.tsx"
      provides: "Profile section wired with EditableNameRow reading/writing display_name setting key"
  key_links:
    - from: "EditableNameRow onBlur / onSubmitEditing"
      to: "setSetting('display_name', trimmedValue)"
      via: "onSave prop callback in settings.tsx that calls setSetting"
      pattern: "setSetting.*display_name"
    - from: "EditableNameRow view mode Pressable"
      to: "isEditing = true + TextInput autoFocus"
      via: "onPress handler sets isEditing state to true"
      pattern: "isEditing.*true"
---

<objective>
Slice 3 of 4: Profile Section — Display Name Inline Edit.

Builds EditableNameRow (the inline view↔edit toggle component patterned after Phase 4's GoalEditor), wires it into the Profile section stub in settings.tsx, and connects to the display_name settings key.

The About section stub in settings.tsx remains empty — it receives content in Plan 04.

Purpose: Delivers SET-02 (user can set a display name). Establishes the inline-edit row pattern.
Output: EditableNameRow.tsx, Profile section in settings.tsx wired to display_name.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/05-notifications-settings/05-CONTEXT.md
@.planning/phases/05-notifications-settings/05-RESEARCH.md
@.planning/phases/05-notifications-settings/05-UI-SPEC.md
@.planning/phases/05-notifications-settings/05-02-SUMMARY.md
</context>

<interfaces>
From GoalEditor.tsx (reference pattern for inline-edit with Reanimated):
  // GoalEditor uses Animated.View with style prop for crossfade from parent
  // EditableNameRow uses same 200ms withTiming opacity transition but is self-contained
  // No external style prop needed — EditableNameRow manages its own animation internally

From src/components/settings/SettingsRow.tsx (created Plan 02):
  interface SettingsRowProps {
    icon?: keyof typeof Ionicons.glyphMap
    label: string
    value?: string
    onPress?: () => void
    disabled?: boolean
    isLast?: boolean
    showChevron?: boolean
    accessibilityLabel?: string
    accessibilityHint?: string
    right?: React.ReactNode
  }

From src/db/repositories/settings.ts:
  export async function getSetting(key: string): Promise<string | null>
  export async function setSetting(key: string, value: string): Promise<void>

Settings key:
  "display_name" → string, empty string when not set, no schema constraint

UI-SPEC Interaction Contract (Display Name):
  - Tap row → label collapses, TextInput appears in place, autoFocus={true}
  - Blur or Return: setSetting("display_name", trimmed_value), return to view mode
  - No Save button, no Cancel button — save is implicit on blur/return
  - Empty after trim → row shows placeholder "Add your name"
  - Transition: 200ms Reanimated withTiming opacity fade between view and edit states

UI-SPEC Row anatomy for name row (view mode):
  accessibilityRole="button"
  accessibilityLabel="Your Name, {display_name || 'not set'}"

UI-SPEC name TextInput (edit mode):
  accessibilityLabel="Your name"
  accessibilityHint="Tap Return to save"

settings.tsx Profile section (to be wired in Task 2):
  SettingsSection title="Profile"
    EditableNameRow  icon=person-outline  label="Your Name"  value={displayName}
                     placeholder="Add your name"
                     onSave={(name) => setSetting('display_name', name)}
                     isLast={true}
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: EditableNameRow component with Reanimated inline-edit</name>
  <files>src/components/settings/EditableNameRow.tsx</files>

  <read_first>
    - src/components/GoalEditor.tsx — reference pattern: Animated.View, withTiming, useAnimatedStyle, opacity fade; note that EditableNameRow is simpler (single-line, no Save/Cancel buttons, no char counter)
    - src/components/settings/SettingsRow.tsx — use as the view-mode row base for visual consistency
    - .planning/phases/05-notifications-settings/05-UI-SPEC.md — Interaction Contract: Display Name section, Row anatomy typography, Accessibility Contract (name row view/edit modes)
    - .planning/phases/05-notifications-settings/05-CONTEXT.md — D-13 (no modal, no Save button, save on blur, mirrors Phase 4 aesthetic)
  </read_first>

  <behavior>
    - In view mode: renders a Pressable row (person-outline icon, "Your Name" label, name value or placeholder "Add your name"). Tapping transitions to edit mode.
    - In edit mode: Pressable is replaced by a View containing a single-line TextInput. TextInput has autoFocus={true}, returnKeyType="done", value=editText. No separate Save/Cancel buttons shown.
    - onBlur: calls onSave(editText.trim()), transitions back to view mode
    - onSubmitEditing: calls onSave(editText.trim()), dismisses keyboard, transitions back to view mode
    - onSave called with empty string when user clears field — parent handles empty (shows placeholder)
    - View↔edit transition: 200ms Reanimated withTiming opacity (view mode fades out, edit mode fades in)
    - View mode row: min-h-[44px] px-4 flex-row items-center gap-3 (matches SettingsRow anatomy)
    - Icon: person-outline, 20px, text-text-secondary
    - Label: "Your Name" font-nunito-bold text-sm text-text-primary flex-1
    - Value (view mode): current name or italic-style placeholder text, font-nunito-regular text-base text-text-secondary
    - TextInput (edit mode): same typography as value text; placeholderTextColor="#8E8E93"; maxLength=50
    - accessibilityRole="button" on view mode Pressable
    - accessibilityLabel={"Your Name, " + (name || "not set")} on view mode Pressable
    - accessibilityLabel="Your name" on TextInput
    - accessibilityHint="Tap Return to save" on TextInput
    - isLast prop: controls border-b divider (same as SettingsRow)
  </behavior>

  <action>
  Create src/components/settings/EditableNameRow.tsx.

  Props interface:
    value: string          // current saved name (empty string = not set)
    onSave: (name: string) => void
    placeholder?: string   // default "Add your name"
    isLast?: boolean       // default false; controls border-b divider

  Component is self-contained for edit state: useState for isEditing (boolean) and editText (string). When transitioning to edit, set editText = current value prop.

  Reanimated: use useAnimatedStyle + withTiming(opacity, {duration: 200}) for the view↔edit fade. Create two Animated.View wrappers — one for the view-mode content, one for the edit-mode TextInput. Both always rendered in the tree (opacity 0/1) to avoid focus timing issues.

  The view mode outer container is a Pressable (to handle tap). The edit mode outer container is a plain View (TextInput handles its own focus). Wrap both in a parent View with overflow: hidden to prevent layout shift.

  Do NOT call setSetting directly inside this component — it receives onSave as a prop and the parent (settings.tsx) owns persistence.

  Import Animated, useAnimatedStyle, withTiming from 'react-native-reanimated'. Import Ionicons from '@expo/vector-icons'. No StyleSheet.create.
  </action>

  <verify>
    <automated>npx jest 2>&1 | tail -5</automated>
  </verify>

  <done>
  src/components/settings/EditableNameRow.tsx exists. npx jest exits 0. Component uses react-native-reanimated for opacity transitions. No StyleSheet.create. Props: value, onSave, placeholder, isLast. TextInput has autoFocus, returnKeyType="done", onBlur and onSubmitEditing both call onSave.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire Profile section into settings.tsx</name>
  <files>app/(tabs)/settings.tsx</files>

  <read_first>
    - app/(tabs)/settings.tsx — current state after Plan 02 Task 3; Profile SettingsSection is a stub with no rows
    - src/components/settings/EditableNameRow.tsx — just created; props: value, onSave, placeholder, isLast
    - src/db/repositories/settings.ts — setSetting signature
    - .planning/phases/05-notifications-settings/05-UI-SPEC.md — Screen Layout Contract (Profile section), Copywriting Contract (name placeholder text)
  </read_first>

  <action>
  Minimal modification to app/(tabs)/settings.tsx — replace the empty Profile SettingsSection with a wired EditableNameRow.

  The displayName state variable is already initialized in the screen's useEffect (it reads 'display_name' from getSetting via Promise.all in Plan 02 Task 3). If not yet present, add 'display_name' to the Promise.all reads and add corresponding useState.

  Inside the Profile SettingsSection (which currently has no children), add:
    EditableNameRow
      value={displayName}
      onSave={async (name) => {
        await setSetting('display_name', name);
        setDisplayName(name);
      }}
      placeholder="Add your name"
      isLast={true}

  Import EditableNameRow from '@/src/components/settings/EditableNameRow'.

  No other changes to the file. The About SettingsSection stub remains empty (Plan 04 fills it).
  </action>

  <verify>
    <automated>npx jest 2>&1 | tail -5</automated>
  </verify>

  <done>
  app/(tabs)/settings.tsx imports and renders EditableNameRow inside Profile SettingsSection. npx jest exits 0. displayName state is loaded from getSetting('display_name') on mount. onSave handler calls setSetting('display_name', name) and updates local state. About section stub is still present (unchanged).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text input → settings DB | display_name is free-text from TextInput before being stored via setSetting |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Tampering | display_name free text input | mitigate | TextInput maxLength=50 enforced at UI layer; setSetting uses Drizzle parameterized query (no SQL injection vector); React Native Text renders as native element (no HTML/XSS) |
| T-05-10 | Information Disclosure | display_name stored in local SQLite | accept | Local-only storage; no network egress; low-sensitivity display string |
</threat_model>

<verification>
After all tasks complete:
1. npx jest exits 0 — full suite green
2. src/components/settings/EditableNameRow.tsx exists
3. grep -c 'EditableNameRow' app/(tabs)/settings.tsx returns >= 1
4. grep -c 'StyleSheet.create' src/components/settings/EditableNameRow.tsx returns 0
5. grep -c 'react-native-reanimated' src/components/settings/EditableNameRow.tsx returns >= 1
6. Manual (EAS dev build): Tap "Your Name" row → TextInput appears with current name prefilled, keyboard shows; type new name and tap Return → row returns to view mode with new name displayed
</verification>

<success_criteria>
- SET-02: User can set a display name via tap-to-edit, saved on blur/return
- Profile section in settings.tsx displays EditableNameRow with display_name persistence
- No guilt language, no friction (no Save/Cancel buttons required)
- npx jest exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/05-notifications-settings/05-03-SUMMARY.md`
</output>
