---
phase: 4
slug: dream-goal
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-12
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for the Dream Goal tab screen. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none (React Native / Expo — shadcn not applicable) |
| Preset | not applicable |
| Component library | NativeWind v4 (NativeWind className utilities; no StyleSheet.create in new components) |
| Icon library | Ionicons via `@expo/vector-icons` (already imported in `_layout.tsx`) |
| Font | Nunito via `@expo-google-fonts/nunito` |

Source: Phase 1 CONTEXT.md D-01, D-02, D-03; tailwind.config.js; theme.ts

---

## Spacing Scale

Declared values (multiples of 4 only):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline element padding (`p-1`, `gap-1`) |
| sm | 8px | Compact spacing, between label and content (`p-2`, `gap-2`) |
| md | 16px | Default horizontal screen padding, element gaps (`px-4`, `gap-4`) |
| lg | 24px | Section vertical padding, card inner padding (`py-6`, `p-6`) |
| xl | 32px | Major content block separation (`mt-8`) |
| 2xl | 48px | Hero section vertical breathing room |
| 3xl | 64px | Page-level top offset (not used in this phase) |

Exceptions:
- Touch targets: minimum 44px height/width for all interactive elements (pencil icon button, Save button, Cancel button). Implemented via `min-h-[44px] min-w-[44px]` as established in `WinInputArea`.
- Character counter: appears only when ≤100 characters remain (`isDirty` + chars ≤100); hidden otherwise.

---

## Typography

| Role | Size | Weight | Line Height | NativeWind class | Usage |
|------|------|--------|-------------|-----------------|-------|
| Body | 16px (text-base) | 400 (regular) | 1.5 (leading-relaxed) | `font-nunito-regular text-base leading-relaxed` | Goal text in view mode, motivational copy, input text |
| Label | 14px (text-sm) | 600 (semibold) | 1.4 (leading-snug) | `font-nunito-semibold text-sm` | Character counter, Cancel button label |
| Heading | 20px (text-xl) | 700 (bold) | 1.2 (leading-tight) | `font-nunito-bold text-xl leading-tight` | Screen section headers, "Your Dream Goal" label |
| Display | 28px (text-[28px]) | 800 (extrabold) | 1.2 (leading-tight) | `font-nunito-extrabold text-[28px] leading-tight` | Goal text rendered as hero in view mode |

Source: Phase 1 CONTEXT.md D-03; `WinCard.tsx`, `StreakHeader.tsx`, `HistoryHeroHeader.tsx` patterns.

Notes:
- Goal text in view mode renders at Display size (28px, extrabold) to establish visual hero weight.
- Motivational copy ("You're building your dream one win at a time") renders at Body size (16px, regular) in `text-text-secondary`.
- TextInput in edit mode uses Body size (16px, regular) matching `WinInputArea` pattern.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#FAF8F4` (`bg-background`) | Screen background, safe area root |
| Secondary (30%) | `#FFFFFF` (`bg-surface`) | GoalCard container surface, TextInput background in edit mode |
| Accent (10%) | `#F5A623` (`bg-primary`) | Save button background only |
| Destructive | none | No destructive actions in this phase |

Accent reserved for: **Save button only** (the single orange CTA element in this phase). The pencil icon and Cancel button do NOT use the accent color.

Additional semantic values in use:
- `#1C1C1E` (`text-text-primary`) — goal text, heading labels
- `#8E8E93` (`text-text-secondary`) — motivational copy, character counter, placeholder text, Cancel button text
- `#F0EDE8` (`border-border`) — card border, TextInput border
- `#FF6B6B` (`text-accent`) — not used in this phase
- `#F7C217` (`text-gold`) — not used in this phase

Pencil icon color: `#8E8E93` (text-secondary) to signal affordance without competing with primary CTA.

Source: Phase 1 CONTEXT.md D-02; `tailwind.config.js`; `theme.ts`; `WinInputArea.tsx` Save button pattern.

---

## Component Inventory

New components to build in this phase:

| Component | File | Description |
|-----------|------|-------------|
| `GoalCard` | `src/components/GoalCard.tsx` | Card surface containing goal hero text + motivational copy in view mode. Analogous to `WinCard` visual pattern: `bg-surface rounded-xl px-4 py-6 shadow-sm`. |
| `GoalEditor` | `src/components/GoalEditor.tsx` | Edit mode: multiline TextInput + character counter + Save/Cancel row. Reuses `WinInputArea` visual patterns. |
| `GoalScreen` | `app/(tabs)/goal.tsx` | Full screen replacement. Orchestrates view/edit toggle, DB load via `useEffect`, dirty-state comparison. |

Existing components / assets referenced (do not rebuild):
- `src/db/repositories/dreamGoal.ts` — `getGoal()` and `upsertGoal(text)` already implemented
- `src/constants/theme.ts` — `Colors` / `Fonts` for non-NativeWind contexts
- `assets/images/trophy.png` — available for empty state illustration (Claude's discretion)
- `Ionicons pencil-outline` — edit affordance icon (D-01 in 04-CONTEXT.md)

---

## Screen Layout Contract

### View Mode (goal exists)

```
SafeAreaView bg-background
  ScrollView px-4
    [Header row — top of scroll area]
      View flex-row items-center justify-between py-4
        Text "Dream Goal"              ← Heading size (20px bold, text-primary)
        Pressable min-h-[44px] min-w-[44px]   ← pencil-outline icon (16px, text-secondary)

    [GoalCard]
      View bg-surface rounded-xl px-4 py-6 shadow-sm border border-border mt-2
        Text goal.text                 ← Display size (28px extrabold, text-primary, leading-tight)

    [Motivational copy — below card]
      Text "You're building your dream one win at a time."
                                       ← Body size (16px regular, text-secondary, text-center, mt-4)
```

### Edit Mode (in-place replacement)

```
SafeAreaView bg-background
  ScrollView px-4
    [Header row]
      View flex-row items-center justify-between py-4
        Text "Dream Goal"              ← Heading size, text-primary
        [no pencil icon in edit mode]

    [GoalEditor]
      View bg-surface rounded-xl px-4 py-4 shadow-sm border border-border mt-2
        TextInput multiline             ← Body size, text-primary, minHeight 120px
          placeholder "What are you working toward?"
          placeholderTextColor #8E8E93
          maxLength={500}
        [Character counter — visible only when ≤100 chars remain]
          Text "{N} / 500"             ← Label size (14px semibold, text-secondary, text-right, mt-1)

    [Action row — below editor, not inside card]
      View flex-row gap-3 mt-4
        Pressable Cancel               ← ghost button, text-secondary, min-h-[44px], flex-1
          Text "Cancel"               ← Label size, text-secondary
        Pressable Save                 ← bg-primary rounded-lg, min-h-[44px], flex-1
          Text "Save Goal"            ← Label size, text-white, font-nunito-bold
          disabled when !isDirty → opacity-50
```

### Empty State (no goal set yet)

```
SafeAreaView bg-background
  ScrollView px-4
    [Motivational copy — above input]
      Text "You're building your dream one win at a time."
                                       ← Body size, text-secondary, text-center, mt-8 mb-6

    [GoalEditor — same component as edit mode]
      [Character counter logic same as edit mode]

    [Save action]
      Pressable Save Goal             ← bg-primary, full-width, min-h-[44px], mt-4
        Text "Save Goal"             ← font-nunito-bold, text-sm, text-white
        disabled when text.trim() === "" → opacity-50
```

### Interaction Transitions

- View mode → Edit mode: triggered by pencil icon tap. Transition: Reanimated `withTiming` opacity fade on GoalCard → GoalEditor swap, duration 200ms ease. No slide — in-place crossfade.
- Edit mode → View mode (Save): save to DB, show updated text immediately, swap back via same 200ms fade.
- Edit mode → View mode (Cancel): silently discard changes (D-02), swap back to GoalCard with last saved text. No confirmation dialog.
- Empty → View mode (after first Save): same 200ms fade, GoalEditor disappears, GoalCard appears.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (save new goal) | "Save Goal" |
| Primary CTA (save edit) | "Save Goal" |
| Cancel action | "Cancel" |
| Edit affordance (accessibility label) | "Edit Dream Goal" |
| Screen section header | "Dream Goal" |
| Empty state motivational copy | "You're building your dream one win at a time." |
| Empty state invitation (placeholder) | "What are you working toward?" |
| Motivational copy (view mode) | "You're building your dream one win at a time." |
| Character counter format | "{N} / 500" (visible only when ≤100 chars remain) |
| Save disabled accessibility hint | "Edit your goal text to enable saving" |

Source: CONTEXT.md D-09 (empty state motivational copy above input), D-07 (character counter), D-03 (motivational copy below in view mode), D-05 (Save button style), D-02 (Cancel silently discards).

No-guilt invariant (CLAUDE.md): zero shame language. Empty state uses invitation framing ("What are you working toward?"), not deficit framing ("You haven't set a goal yet").

No destructive actions in Phase 4. Delete goal is not in scope. Cancel is non-destructive (in-session discard only).

---

## Accessibility Contract

| Element | Requirement |
|---------|------------|
| Pencil icon button | `accessibilityRole="button"` + `accessibilityLabel="Edit Dream Goal"` |
| Save button | `accessibilityRole="button"` + `accessibilityLabel="Save Goal"` + `accessibilityState={{ disabled: !isDirty }}` |
| Cancel button | `accessibilityRole="button"` + `accessibilityLabel="Cancel editing"` |
| GoalCard (view mode) | `accessibilityLabel={goal.text}` |
| TextInput (edit mode) | `accessibilityLabel="Dream Goal text"` + `accessibilityHint="Type your dream goal, up to 500 characters"` |
| Character counter | `accessibilityLabel="{N} of 500 characters used"` (only when rendered) |

Minimum touch target: 44×44px for all interactive elements (Pressables with `min-h-[44px] min-w-[44px]`).

---

## State Machine

| State | Trigger | UI |
|-------|---------|-----|
| `loading` | mount | SafeAreaView bg-background, empty (no flash of empty state) |
| `empty` | load returns null | Empty state layout (motivational copy above, TextInput below) |
| `view` | load returns text, or after Save | GoalCard with hero text + motivational copy below, pencil icon |
| `editing` | pencil icon tap | GoalEditor in-place, Save + Cancel row |
| `saving` | Save pressed | Save button opacity-50 + disabled during async `upsertGoal()` call |

State variable: local `useState` with `'loading' | 'empty' | 'view' | 'editing' | 'saving'` discriminated union.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable (React Native project) |
| Third-party | none | not applicable |

This is a React Native / Expo project. shadcn and web component registries are not used. NativeWind v4 provides the styling layer. All components are hand-authored.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
