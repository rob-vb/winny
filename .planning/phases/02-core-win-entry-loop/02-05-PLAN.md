---
phase: 02-core-win-entry-loop
plan: 05
type: execute
wave: 4
depends_on:
  - "02-04"
files_modified: []
autonomous: false
requirements:
  - WIN-01
  - WIN-02
  - WIN-03
  - WIN-04
  - STREAK-01
  - STREAK-02
  - STREAK-03
  - STREAK-04

must_haves:
  truths:
    - "User sees trophy + 'What was your win today?' on first open (empty state)"
    - "User types a win, taps Add Win, win appears in list with visible animation"
    - "User types a second win, taps Add Win, both wins visible — no navigation required"
    - "Streak header shows correct streak count and encouraging label (not 'Loading...')"
    - "Example prompts are visible: 3 muted 'e.g. ...' lines above input"
    - "Keyboard stays open after add — user can immediately type next win"
    - "Total wins count increments after each add"
    - "Unit test suite passes: `npx jest --no-coverage` exits 0"
  artifacts:
    - path: "src/__tests__/streakLabel.test.ts"
      provides: "STREAK-04 no-guilt audit — automated"
    - path: "src/__tests__/promptUtils.test.ts"
      provides: "WIN-02 determinism — automated"
    - path: "app/(tabs)/index.tsx"
      provides: "Full Home screen — functional Phase 2 value loop"
      contains: "useWinsStore"
  key_links:
    - from: "app/(tabs)/index.tsx"
      to: "SQLite DB"
      via: "store.addWin → insertWin → wins table"
      pattern: "addWin"
    - from: "app/(tabs)/index.tsx"
      to: "streakLabel"
      via: "StreakHeader → streakLabel(streak)"
      pattern: "streakLabel"
---

<objective>
Human verification checkpoint for Phase 2. Confirm the complete core value loop works end-to-end on a real device or simulator.

All automated checks (TypeScript, Jest unit tests) run first. Then the user verifies the interactive behaviors that require visual and tactile confirmation: animation, keyboard behavior, state transitions, streak display.

Purpose: Automated tests cannot catch: wrong animation direction, keyboard covering input, visual regressions, or the "feel" of the micro-interaction. Human eyes are the final gate.

WIN-04 override: Confirmed by observation — no "I'm done" button, always-open session model works as designed (D-03).

Output: Go / No-Go decision for Phase 2. Issues raised here become gap closure targets.
</objective>

<execution_context>
@/Users/rob/.claude/get-shit-done/workflows/execute-plan.md
@/Users/rob/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-core-win-entry-loop/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run all automated checks</name>
  <files></files>
  <read_first>
    - (no files to read — this task runs commands only)
  </read_first>
  <action>
    Run these three checks in order. All must pass before proceeding to the human verification checkpoint.

    1. TypeScript compilation:
    ```bash
    npx tsc --noEmit
    ```
    Expected: exits 0, no output.

    2. Unit test suite:
    ```bash
    npx jest --no-coverage
    ```
    Expected: exits 0, all tests pass.

    3. Key wiring grep checks:
    ```bash
    # Store connected to Home screen
    grep -c "useWinsStore" app/\(tabs\)/index.tsx

    # Trophy asset exists
    ls -la assets/images/trophy.png

    # WIN-04 override documented
    grep -c "WIN-04 override" app/\(tabs\)/index.tsx

    # No behavior="height" (Android Pitfall 3)
    grep -v '^//' app/\(tabs\)/index.tsx | grep -c '"height"'

    # No blur() call (keyboard stays open)
    grep -v '^//' app/\(tabs\)/index.tsx | grep -c "blur"

    # Prompt pool size (>= 40 prompts)
    grep -v '^//' src/constants/examplePrompts.ts | grep -c '"I '

    # No guilt language in tier-0 streak label
    node -e "const {streakLabel} = require('./src/utils/streakLabel'); const label = streakLabel(0); const hasGuilt = /miss|fail|broke|punish|shame|sorry|oops/i.test(label); console.log('STREAK-04:', hasGuilt ? 'FAIL' : 'PASS', label);"
    ```

    Expected values:
    - `useWinsStore` grep: >= 1
    - trophy.png: file exists
    - `WIN-04 override` grep: >= 1
    - `"height"` grep (filtered): 0
    - `blur` grep (filtered): 0
    - prompt pool grep: >= 40
    - STREAK-04 node check: "PASS Start your streak today! 🌟"

    If ANY check fails: fix before proceeding to the checkpoint. Do not present the checkpoint if automated checks fail.
  </action>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npx jest --no-coverage` exits 0 with 0 failures
    - All grep checks return expected values
    - STREAK-04 node check outputs "PASS"
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest --no-coverage 2>&1 | tail -5</automated>
  </verify>
  <done>
    Both `tsc` and `jest` exit 0. All grep checks pass. STREAK-04 verified clean.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — complete core value loop</name>
  <what-built>
    Phase 2 Core Win-Entry Loop:
    - Home tab fully replaced with streak header + win list + prompts + pinned input
    - Zustand v5 store hydrates from SQLite on mount
    - Win entry with 1–200 char validation and ZoomIn.duration(300) animation
    - Daily-rotating example prompts (3 muted lines, "e.g. " prefix)
    - Streak calculation and encouraging tier labels (no guilt language)
    - Empty state (trophy + heading) ↔ Populated state (FlatList) transition
    - Keyboard stays open after Add Win — no blur()
    - WIN-04 satisfied via D-03 always-open model (no "I'm done" button)
  </what-built>
  <how-to-verify>
    Open Expo Go on your simulator or device and verify ALL of the following:

    **A. Fresh empty state**
    1. Close and reopen the app (or use a fresh install)
    2. Navigate to the Home tab
    3. Confirm: trophy image is visible (centered, ~120px wide)
    4. Confirm: "What was your win today?" heading is visible below the trophy
    5. Confirm: 3 muted "e.g. ..." example prompt lines are visible above the input
    6. Confirm: input field is auto-focused (keyboard is open on load)
    7. Confirm: Add Win button shows "Add Win" label (not just "Add")

    **B. Streak header**
    8. Confirm: streak header is visible at the very top (above the trophy/list)
    9. Confirm: the streak label is encouraging — e.g. "Start your streak today! 🌟" (not a number alone, not shame language)
    10. Confirm: "0 total wins" (or current count) appears below the streak label in muted text

    **C. First win entry**
    11. Type a win in the input (e.g. "I shipped Phase 2 today!")
    12. Tap "Add Win" — confirm:
        - The win card appears at the TOP of the screen (above older wins if any)
        - A brief scale/fade animation plays on the new card (ZoomIn ~300ms)
        - The input field is cleared
        - The keyboard STAYS OPEN (crucial — do not call blur)
        - The trophy/empty-state heading disappears; FlatList appears instead

    **D. Multiple wins in one session**
    13. Without navigating away, type a second win and tap Add Win
    14. Confirm: both wins visible in the list (newest at top), no navigation required
    15. Confirm: total wins counter in the header incremented by 2

    **E. Streak update**
    16. Confirm: streak label has updated to reflect the first win of the day
    17. If you previously had no wins today: streak should now show "Day 1! Every streak starts here. 🎉" or your current streak count

    **F. Example prompts**
    18. Confirm: the 3 "e.g. ..." prompt lines are NOT tappable (no active state, no navigation)
    19. Confirm: prompts appear above the input field in both empty and populated states

    **G. Keyboard behavior**
    20. Open the app, focus the input, scroll the win list — confirm the input area stays above the keyboard
    21. Type and add a win — confirm keyboard stays open and input is cleared but focused

    **H. STREAK-04 no-guilt check**
    22. Note the current streak label text — confirm it contains no words like: "missed", "broke", "failed", "shame", "punish"
  </how-to-verify>
  <resume-signal>
    Type "approved" if all checks pass.
    Or describe any issues found (e.g., "C12: animation did not fire", "G20: keyboard covers input on Android").
    Issues will be addressed in gap closure before Phase 3.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human observation → gap closure | Failures identified here become structured gap closure tasks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-01 | Tampering | Manual test data | accept | Test data entered during verification is real app data; streak count may increment; this is correct behavior |
</threat_model>

<verification>
All automated checks completed before checkpoint (Task 1).

Human verification covers 22 specific observable behaviors across 8 categories (A–H).

Phase 2 success criteria (from ROADMAP.md):
1. User opens app and sees "What was your win today?" prompt with 3 example prompts that do not change mid-day — VERIFIED by checks A3-A6
2. User types a win, taps add, types a second win, and both appear in the session without navigating away — VERIFIED by checks C11-D14
3. User taps "I'm done for today" — OVERRIDDEN by D-03; always-open model verified by absence of button and inline session visibility
4. Home screen shows a streak count correctly reflecting consecutive calendar days — VERIFIED by checks B8-B10, E15-E16
5. Missing a full calendar day resets streak to 0; total wins never decreases — Logic in computeStreak (verified by unit tests); observable via streak header
</verification>

<success_criteria>
Phase 2 is complete when:
1. All automated checks in Task 1 pass (tsc, jest, grep invariants)
2. Human verification in Task 2 confirms all 22 observable behaviors
3. No blocking issues identified (or all blocking issues resolved via gap closure)
</success_criteria>

<output>
After completion (human approval received), create `.planning/phases/02-core-win-entry-loop/02-05-SUMMARY.md`

Also update REQUIREMENTS.md to note: "WIN-04 — Overridden by D-03. No session-lock button. Always-open calendar-day model satisfies intent: today's wins always visible inline."
</output>
