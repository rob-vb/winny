---
phase: 07-eas-build-app-store-submission
plan: "04"
type: execute
wave: 3
depends_on:
  - "07-03"
files_modified:
  - eas.json
autonomous: false
requirements: []

user_setup:
  - service: apple_developer
    why: "App Store Connect app record must be created manually in the web UI before eas submit can reference an ascAppId"
    env_vars: []
    dashboard_config:
      - task: "Create app record in App Store Connect"
        location: "appstoreconnect.apple.com → My Apps → + → New App → fill name=Winny, bundle ID=com.robvb.winny, SKU=winny-ios"
      - task: "Copy the numeric Apple ID from App Store Connect"
        location: "App Store Connect → Winny app → App Information (left sidebar) → Apple ID field (numeric, e.g. 1234567890)"
      - task: "Enter app metadata in App Store Connect"
        location: "App Store Connect → Winny → App Store tab → Version Information"

must_haves:
  truths:
    - "App Store Connect has a Winny app record with bundle ID com.robvb.winny"
    - "eas.json submit.production.ios.ascAppId is updated with the real numeric Apple ID (not the placeholder)"
    - "iOS TestFlight build is uploaded and available for internal testing in App Store Connect"
    - "App Store listing metadata (description, screenshots, category, privacy URL) is entered in App Store Connect"
    - "User has installed the TestFlight build and completed the smoke test"
  artifacts:
    - path: "eas.json"
      provides: "ascAppId updated with real numeric Apple ID from App Store Connect"
  key_links:
    - from: "eas submit --platform ios"
      to: "App Store Connect TestFlight"
      via: "EAS Submit using ascAppId from eas.json"
      pattern: "human-verified via App Store Connect dashboard"
---

<objective>
Set up the App Store Connect app record, upload the iOS binary to TestFlight via EAS Submit, enter store listing metadata, and run the TestFlight smoke test. This wave gates the iOS review submission.

Purpose: The ascAppId only exists after manual app record creation in App Store Connect — this human step cannot be automated. Once the record exists, EAS Submit handles the binary upload. The smoke test confirms the production binary works end-to-end before submitting for Apple review.
Output: eas.json with real ascAppId, iOS binary on TestFlight, metadata entered, smoke test passed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-eas-build-app-store-submission/07-CONTEXT.md
@.planning/phases/07-eas-build-app-store-submission/07-RESEARCH.md
@.planning/phases/07-eas-build-app-store-submission/07-03-SUMMARY.md
@.planning/phases/07-eas-build-app-store-submission/store-listing-content.md
@.planning/phases/07-eas-build-app-store-submission/legal-content.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create App Store Connect app record + copy ascAppId</name>

  <what-built>
  EAS Submit requires an ascAppId — the numeric Apple ID from App Store Connect — before it can upload a binary. This ID only exists after you manually create the app record in the App Store Connect web UI. This is a one-time manual action; all subsequent binary uploads use eas submit (automated).
  </what-built>

  <how-to-verify>
  1. Open https://appstoreconnect.apple.com
  2. Sign in as rbvbaaren@gmail.com
  3. Click "My Apps" → "+" → "New App"
  4. Fill in:
     - Platforms: iOS
     - Name: Winny
     - Primary Language: English (or your preferred locale)
     - Bundle ID: com.robvb.winny (must match app.json exactly — select from dropdown or type)
     - SKU: winny-ios (any unique identifier)
     - User Access: Full Access
  5. Click Create
  6. In the app record, go to App Information in the left sidebar (under General)
  7. Find the "Apple ID" field — it is a numeric value like 1234567890
  8. Copy this number exactly

  The ascAppId is now known.

  Next action for Claude (after you provide the number): Claude will update eas.json submit.production.ios.ascAppId with the real value and run eas submit.
  </how-to-verify>

  <resume-signal>Provide the numeric Apple ID (ascAppId) from App Store Connect. Example: "Apple ID is 1234567890"</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update eas.json ascAppId + submit iOS binary to TestFlight</name>
  <files>eas.json</files>

  <read_first>
    - eas.json — current state: submit.production.ios.ascAppId is set to the placeholder string from Plan 01; replace with the real numeric ID provided by the user in Task 1
    - .planning/phases/07-eas-build-app-store-submission/07-RESEARCH.md — Pitfall 3 (ascAppId not available until after app created — describes exactly this flow), "EAS Build → Submit Pipeline" architecture section
    - .planning/phases/07-eas-build-app-store-submission/07-03-SUMMARY.md — EAS build IDs for the iOS production build (needed to reference the correct build in eas submit if EAS prompts for build selection)
  </read_first>

  <action>
  Step 1 — Edit eas.json: replace the ascAppId placeholder string with the real numeric Apple ID provided by the user in Task 1. The field is at submit.production.ios.ascAppId. Replace whatever placeholder value is there with the numeric string exactly as provided (do not add quotes inside the string — it is already a string field in JSON).

  Step 2 — Verify the eas.json change:
    node -e "const e=require('./eas.json'); console.log(e.submit.production.ios.ascAppId)"
  Confirm it prints the numeric ID, not a placeholder.

  Step 3 — Submit the iOS production build to App Store Connect (TestFlight):
    eas submit --platform ios --profile production --latest

  The --latest flag uses the most recent successful production build from EAS. If EAS prompts to select a build, choose the most recent "finished" production iOS build (from Plan 03).

  EAS Submit authenticates with Apple using the appleId (rbvbaaren@gmail.com) and ascAppId from eas.json. The binary uploads to App Store Connect and appears in TestFlight within 10–20 minutes.

  If authentication prompts for an Apple ID password or 2FA: enter credentials interactively. EAS stores the session token so subsequent submits don't require re-authentication.
  </action>

  <verify>
    <automated>node -e "const e=require('./eas.json'); const id=e.submit.production.ios.ascAppId; console.log(id && !id.includes('PLACEHOLDER') ? 'OK: ' + id : 'STILL A PLACEHOLDER')"</automated>
    <automated>eas submit:list --platform ios --limit 3 2>&1 | head -20</automated>
  </verify>

  <done>
  eas.json submit.production.ios.ascAppId contains a real numeric Apple ID (not the placeholder string). eas submit completed without error. eas submit:list shows a completed iOS submission. The binary appears in App Store Connect TestFlight within ~20 minutes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: App Store metadata entry + TestFlight smoke test</name>

  <what-built>
  The iOS production binary is now available in TestFlight (from Task 2). Store listing metadata needs to be entered in App Store Connect, and the TestFlight build needs a smoke test on a physical iPhone before submitting for App Review.
  </what-built>

  <how-to-verify>
  Part A — Enter App Store metadata in App Store Connect:

  1. Open https://appstoreconnect.apple.com → Winny → App Store tab → "1.0 Prepare for Submission"
  2. Enter metadata from store-listing-content.md (review that file first):
     - App name: Winny — Daily Win Streak
     - Subtitle: "Log wins. Build your streak." (30 char limit)
     - Description: (long description from store-listing-content.md)
     - Keywords: (keyword set from store-listing-content.md, within 100 char limit)
     - Support URL: [YOUR_DOMAIN]/support (or same as privacy URL for now)
     - Privacy Policy URL: [the URL you decided in Plan 02 OQ-2]
  3. Upload screenshots (per guidance in store-listing-content.md):
     - Capture 5 screens on iPhone 16 Pro Max Simulator at 1260×2736
     - Upload in App Store Connect → Screenshots section → 6.9" Display
  4. Content rights: select "I have the rights to use..." (this is your app)
  5. Age rating: complete the questionnaire (answer No to all — no user-generated content, no gambling, no mature content)
  6. Category: Health & Fitness

  Part B — TestFlight internal smoke test:

  1. In App Store Connect → TestFlight tab, confirm the build is available (may show "Processing" for up to 20 min after upload)
  2. Install TestFlight on your iPhone if not already installed
  3. Add yourself as an internal tester in App Store Connect → TestFlight → Internal Testing → add your Apple ID
  4. Open TestFlight on iPhone → install Winny
  5. Run smoke test (3 steps):
     a. Fresh install: Welcome/onboarding screen appears → tap through → reach win entry screen. No crash.
     b. Log one win → notification permission dialog appears (NOTF-01 confirmed again in production binary)
     c. Settings → reminder time picker works → display name saves → How It Works screen opens
  6. Confirm: no crashes, no visual regressions, onboarding flows to win entry correctly
  </how-to-verify>

  <resume-signal>Type "approved" after metadata entry and smoke test both pass. Or describe any failures (crash logs, visual issues, missing metadata fields).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Apple Developer account → App Store Connect | Binary upload and metadata; Apple controls distribution |
| TestFlight build → user device | Pre-release distribution; not public; internal tester only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Spoofing | Apple ID authentication in eas submit | accept | EAS Submit uses Apple's OAuth; session token stored securely by EAS CLI; 2FA enforced by Apple |
| T-07-11 | Denial of Service | Wrong ascAppId causes eas submit failure | mitigate | Verify ascAppId is numeric (not placeholder) before running eas submit; grep check in Task 2 automated verify |
| T-07-12 | Information Disclosure | Privacy policy URL must be live before submission | mitigate | Privacy policy URL required in metadata before submitting for review; 404 = automatic rejection (Pitfall 6 in RESEARCH.md) |
</threat_model>

<verification>
After Task 3 checkpoint:
1. eas.json submit.production.ios.ascAppId contains a real numeric ID (not placeholder)
2. App Store Connect shows Winny app with status "Prepare for Submission"
3. All metadata fields filled: name, subtitle, description, keywords, screenshots, privacy URL, age rating, category
4. TestFlight build shows status "Ready to Test" in App Store Connect
5. Smoke test: onboarding → first win → permission dialog → settings all pass on physical device
</verification>

<success_criteria>
- App Store Connect Winny app record exists with bundle ID com.robvb.winny (Pitfall 8 prevented — correct bundle ID locked in)
- TestFlight binary live and smoke-tested (D-07 iOS verification)
- Store metadata entered and ready for review submission in Wave 4
- eas.json ascAppId updated from placeholder to real value
</success_criteria>

<output>
After completion, create `.planning/phases/07-eas-build-app-store-submission/07-04-SUMMARY.md`
Include: ascAppId value, TestFlight build version, smoke test pass/fail status, any metadata notes for Wave 4.
</output>
