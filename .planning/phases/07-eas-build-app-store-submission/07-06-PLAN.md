---
phase: 07-eas-build-app-store-submission
plan: "06"
type: execute
wave: 4
depends_on:
  - "07-04"
  - "07-05"
files_modified:
  - src/constants/links.ts
autonomous: false
requirements: []

must_haves:
  truths:
    - "Privacy policy and terms of use pages are live at the URLs referenced in links.ts and return HTTP 200"
    - "links.ts PRIVACY_URL, TERMS_URL, and APP_STORE_URL contain the real (non-placeholder) live URLs"
    - "iOS app is submitted for App Store review from App Store Connect"
    - "Android app is promoted to Closed Testing (if tester gate applies) OR submitted for production review (if no gate)"
    - "Both review submissions are in status 'Waiting for Review' or equivalent"
  artifacts:
    - path: "src/constants/links.ts"
      provides: "Final live URLs: PRIVACY_URL, TERMS_URL pointing to live pages; APP_STORE_URL with real Apple App ID"
  key_links:
    - from: "PRIVACY_URL in links.ts"
      to: "Live privacy policy page returning HTTP 200"
      via: "curl -I <PRIVACY_URL>"
      pattern: "HTTP/2 200"
    - from: "APP_STORE_URL in links.ts"
      to: "Real App Store listing URL"
      via: "apps.apple.com/app/winny/id<ascAppId>"
      pattern: "ascAppId from Plan 04"
---

<objective>
Verify legal pages are live, update links.ts with final URLs, submit iOS for App Store review, and submit Android to the appropriate track (production or closed testing depending on tester gate). This plan completes the submission gate.

Purpose: App Store reviewers test every URL in the app and metadata — a 404 on the privacy policy is an automatic rejection. Links.ts must be updated to real URLs before review begins. Both store submissions are triggered here.
Output: links.ts with live URLs committed, iOS in App Review queue, Android in review/promotion queue.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-eas-build-app-store-submission/07-CONTEXT.md
@.planning/phases/07-eas-build-app-store-submission/07-RESEARCH.md
@.planning/phases/07-eas-build-app-store-submission/07-04-SUMMARY.md
@.planning/phases/07-eas-build-app-store-submission/07-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Publish legal pages + confirm URL reachability</name>

  <what-built>
  Legal page text was drafted in Plan 01 and is in .planning/phases/07-eas-build-app-store-submission/legal-content.md. The user hosts these pages — Claude cannot publish them. This task confirms the pages are live before links.ts is updated with the real URLs.
  </what-built>

  <how-to-verify>
  1. Review legal-content.md and fill in all [PLACEHOLDER] items:
     - [DATE]: today's date (e.g., "May 14, 2026")
     - [YOUR_EMAIL]: rbvbaaren@gmail.com
     - [YOUR_JURISDICTION]: your country/state (e.g., "The Netherlands")
  2. Publish the Privacy Policy at your chosen domain/path (per OQ-2 answer from Plan 02):
     Example: winny.app/privacy OR winningstreak.app/privacy
  3. Publish the Terms of Use at the corresponding path:
     Example: winny.app/terms OR winningstreak.app/terms
  4. Verify both URLs are reachable:
     curl -I https://[YOUR_DOMAIN]/privacy   → should return HTTP 200 (not 404, not redirect to homepage)
     curl -I https://[YOUR_DOMAIN]/terms     → same

  Provide the two live URLs in the resume signal.
  </how-to-verify>

  <resume-signal>Provide the two live URLs: "Privacy: https://... | Terms: https://..."</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Update links.ts with live URLs + verify reachability</name>
  <files>src/constants/links.ts</files>

  <read_first>
    - src/constants/links.ts — current state: PRIVACY_URL and TERMS_URL are winny.app placeholders, APP_STORE_URL is a placeholder with id000000000, SHARE_MESSAGE is already updated to "Winny" (from Plan 01)
    - .planning/phases/07-eas-build-app-store-submission/07-04-SUMMARY.md — contains the real ascAppId (numeric Apple ID) for constructing APP_STORE_URL
    - .planning/phases/07-eas-build-app-store-submission/07-RESEARCH.md — Pitfall 6 (stub URLs cause App Store rejection — URL must return 200 before submission)
  </read_first>

  <action>
  Edit src/constants/links.ts with the following changes. Use the exact URLs provided by the user in Task 1 for PRIVACY_URL and TERMS_URL:

    PRIVACY_URL: replace the winny.app placeholder with the live URL from Task 1 (e.g., "https://winny.app/privacy" or "https://winningstreak.app/privacy")
    TERMS_URL: replace the winny.app placeholder with the live URL from Task 1
    APP_STORE_URL: construct from the ascAppId in Plan 04 SUMMARY. Format: "https://apps.apple.com/app/winny/id<NUMERIC_ID>" (replace <NUMERIC_ID> with the real number). Note: the App Store URL may not resolve until the app is approved — the format is correct, reachability check will return 404 until approval, which is expected at this stage.
    SHARE_MESSAGE: already correct ("I've been using Winny...") — do not change.

  After editing, run reachability checks:
    curl -I <PRIVACY_URL> 2>&1 | head -3
    curl -I <TERMS_URL> 2>&1 | head -3

  Both must return HTTP 200 (or a redirect that resolves to 200). If either returns non-200, do not proceed — the legal pages are not live yet. Return to Task 1.

  Then check that no old "Just Keep Winning" or "winningstreak.app" placeholder strings remain in links.ts:
    grep -n "Just Keep Winning\|winningstreak.app\|id000000000" src/constants/links.ts
  Should return no matches (if placeholder APP_STORE_URL id000000000 was replaced with the real ascAppId).
  </action>

  <verify>
    <automated>node -e "const l=require('./src/constants/links'); console.log(l.PRIVACY_URL, l.TERMS_URL, l.APP_STORE_URL, l.SHARE_MESSAGE)"</automated>
    <automated>curl -I $(node -e "const l=require('./src/constants/links'); process.stdout.write(l.PRIVACY_URL)") 2>&1 | head -3</automated>
    <automated>curl -I $(node -e "const l=require('./src/constants/links'); process.stdout.write(l.TERMS_URL)") 2>&1 | head -3</automated>
    <automated>grep -n "Just Keep Winning\|winningstreak.app\|id000000000" src/constants/links.ts && echo "STALE PLACEHOLDERS FOUND" || echo "OK — no stale placeholders"</automated>
  </verify>

  <done>
  src/constants/links.ts: PRIVACY_URL and TERMS_URL are live URLs returning HTTP 200. APP_STORE_URL contains the real ascAppId numeric ID. SHARE_MESSAGE says "Winny". No stale placeholder strings remain. D-10 satisfied.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Submit for App Store review + Android track promotion/submission</name>

  <what-built>
  All prerequisites are now met:
  - Legal pages are live (Task 1 + 2)
  - links.ts contains real URLs (Task 2)
  - iOS metadata entered and TestFlight smoke test passed (Plan 04)
  - Android metadata entered and Internal Test smoke test passed (Plan 05)
  Both stores are ready for review submission.
  </what-built>

  <how-to-verify>
  Part A — iOS App Store review submission:

  1. Open App Store Connect → Winny → App Store → Version 1.0
  2. Scroll to the top of the version page and verify all sections show a checkmark:
     - App Information
     - App Privacy
     - Version Information (description, screenshots, privacy URL)
     - Build (TestFlight build should be selected)
  3. Update the privacy policy URL in App Store Connect to the live URL from Task 2 (if it was a placeholder before)
  4. In "Review Information":
     - Sign-in required: No
     - Demo account: not needed (app has no login)
     - Notes for reviewer: "This is a local habit-tracking app. No server-side data collection. Push notification permission is requested only after the user logs their first win (not on app launch). All data is stored locally on device."
  5. Click "Submit for Review" (or "Add for Review" → "Submit to App Review")
  6. Confirm status changes to "Waiting for Review"

  Part B — Android submission (per tester gate status from Plan 05):

  If NO tester gate (account created before Nov 13, 2023):
    Option A (eas submit — now works since app record exists):
      eas submit --platform android --profile production --latest
    Option B (manual via Play Console):
      Play Console → Production → Create new release → promote from Internal Testing → Submit for review
    Confirm status: "In review" in Play Console

  If YES tester gate (account created after Nov 13, 2023):
    Play Console → Testing → Closed testing → Create track (if not already done in Plan 05)
    Add 12 testers to the closed testing track and get them to opt in
    The 14-day clock starts when the first tester installs the app
    NOTE: Android production review cannot proceed until 14 days after at least 12 testers are opted in. Plan 07 will be blocked for Android until that gate clears. iOS review can proceed in parallel.

  Confirm in resume signal:
  - iOS: "Waiting for Review" status in App Store Connect
  - Android (no gate): "In review" in Play Console
  - Android (gate): "Closed testing track created, N/12 testers opted in, clock started [DATE]"
  </how-to-verify>

  <resume-signal>Type "submitted" and include: iOS status in App Store Connect + Android status (in review OR closed testing clock started with tester count and start date).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| links.ts URLs → app users | URLs are hardcoded in the binary; wrong URLs persist until next OTA update or new binary |
| App Store Connect → Apple Review team | Metadata and binary reviewed for guideline compliance |
| Google Play Console → Google Review team | Binary reviewed for policy compliance |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-17 | Denial of Service | Stub privacy URL at review time | mitigate | Automated curl check in Task 2 verifies HTTP 200 before any review submission proceeds; Pitfall 6 followed |
| T-07-18 | Information Disclosure | Legal pages expose hosting infrastructure | accept | Static pages on user's domain; acceptable risk for a public app |
| T-07-19 | Tampering | links.ts wrong APP_STORE_URL after binary ships | accept | APP_STORE_URL used only for "Rate App" fallback and Share message; can be corrected via OTA update (EAS Update) if needed |
</threat_model>

<verification>
After all tasks:
1. curl -I <PRIVACY_URL> returns HTTP 200
2. curl -I <TERMS_URL> returns HTTP 200
3. grep -n "Just Keep Winning\|id000000000" src/constants/links.ts returns no lines
4. App Store Connect shows Winny version 1.0 in "Waiting for Review" status
5. Android: Play Console shows either "In review" (no gate) or closed testing track with tester count (gate applies)
</verification>

<success_criteria>
- D-10 satisfied: links.ts PRIVACY_URL, TERMS_URL, APP_STORE_URL all contain real live URLs (not placeholders)
- Both legal pages return HTTP 200 (Pitfall 6 prevented)
- iOS submitted for App Store review (gates phase success criterion 1)
- Android in review or on 14-day closed test clock (gates phase success criterion 2)
</success_criteria>

<output>
After completion, create `.planning/phases/07-eas-build-app-store-submission/07-06-SUMMARY.md`
Include: live privacy/terms URLs, App Store Connect review status, Android review/gate status, date of submissions.
</output>
