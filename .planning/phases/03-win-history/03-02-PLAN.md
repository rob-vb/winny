---
phase: 03-win-history
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/HistoryHeroHeader.tsx
  - src/components/DateSectionHeader.tsx
autonomous: true
requirements:
  - HIST-02
  - HIST-03

must_haves:
  truths:
    - "HistoryHeroHeader renders a 64px gold number (totalWins), 'total wins' sub-label, streakLabel(streak) line, and trophy image"
    - "DateSectionHeader renders a date label (left), win-count pill badge (right), and animated chevron (far right)"
    - "DateSectionHeader chevron rotates 180° when isCollapsed=true"
    - "The entire DateSectionHeader row is tappable via Pressable calling onToggle"
    - "DateSectionHeader is wrapped in React.memo"
    - "Both components use NativeWind className only — no StyleSheet.create"
  artifacts:
    - path: "src/components/HistoryHeroHeader.tsx"
      provides: "Hero stats header component for ListHeaderComponent"
      exports: ["HistoryHeroHeader"]
    - path: "src/components/DateSectionHeader.tsx"
      provides: "Collapsible section header for renderSectionHeader"
      exports: ["DateSectionHeader"]
  key_links:
    - from: "src/components/HistoryHeroHeader.tsx"
      to: "src/utils/streakLabel.ts"
      via: "import { streakLabel } from '@/src/utils/streakLabel'"
      pattern: "streakLabel"
    - from: "src/components/DateSectionHeader.tsx"
      to: "src/utils/dateUtils.ts"
      via: "import { formatDateKey } from '@/src/utils/dateUtils'"
      pattern: "formatDateKey"
    - from: "src/components/DateSectionHeader.tsx"
      to: "react-native-reanimated"
      via: "useSharedValue + useAnimatedStyle + withTiming"
      pattern: "useSharedValue"
---

<objective>
Create the two new presentational components for the My Wins screen: HistoryHeroHeader (the scrolling trophy + total-wins stat block) and DateSectionHeader (the sticky collapsible section header with animated chevron). Both components are pure presentational — they receive all data via props and have no store connections.

Purpose: These are the visual building blocks that wins.tsx will assemble. Building them first in isolation makes the screen assembly in Plan 03 straightforward — clear contracts, no surprises.

Output:
- src/components/HistoryHeroHeader.tsx — new file
- src/components/DateSectionHeader.tsx — new file
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-win-history/03-CONTEXT.md
@.planning/phases/03-win-history/03-RESEARCH.md
@.planning/phases/03-win-history/03-PATTERNS.md
@.planning/phases/03-win-history/03-UI-SPEC.md
</context>

<interfaces>
From src/components/StreakHeader.tsx (analog for HistoryHeroHeader):
```typescript
import { View, Text, Image } from "react-native";
import { streakLabel } from "@/src/utils/streakLabel";

interface StreakHeaderProps {
  streak: number;
  totalWins: number;
}
export function StreakHeader({ streak, totalWins }: StreakHeaderProps) { ... }
// Uses: trophy.png at 48×48, flex-row items-center px-4 py-6 border-b border-border bg-background
```

From src/components/WinCard.tsx (Reanimated pattern analog):
```typescript
import Animated, { ZoomIn } from "react-native-reanimated";
// DateSectionHeader uses: useSharedValue, useAnimatedStyle, withTiming instead of ZoomIn
```

From src/components/WinInputArea.tsx (Pressable pattern analog):
```typescript
<Pressable onPress={...} accessibilityRole="button" accessibilityLabel="..." className="...">
```

From src/utils/streakLabel.ts:
```typescript
export function streakLabel(streak: number): string
// Returns string like "12 day streak! You're on fire! 🔥"
```

From src/utils/dateUtils.ts (will exist after Plan 01):
```typescript
export function formatDateKey(dateKey: string): string
// Returns: "Today" | "Yesterday" | "EEE, MMM d" | "MMM d, yyyy"
```

WinSection interface (define locally in DateSectionHeader.tsx):
```typescript
interface WinSection {
  date_key: string;
  data: { id: string; [key: string]: unknown }[];
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: HistoryHeroHeader component</name>
  <files>src/components/HistoryHeroHeader.tsx</files>

  <read_first>
    - src/components/StreakHeader.tsx — the analog; HistoryHeroHeader is column-stack vs StreakHeader's flex-row
    - .planning/phases/03-win-history/03-PATTERNS.md — "HistoryHeroHeader.tsx (component, props)" section with full pattern including exact class names
    - .planning/phases/03-win-history/03-UI-SPEC.md — "HistoryHeroHeader Component" section (all class names, sizes, typography)
    - src/utils/streakLabel.ts — import target; confirm export name
  </read_first>

  <behavior>
    - HistoryHeroHeaderProps: { totalWins: number; streak: number }
    - Outer View: className="items-center px-4 py-6 border-b border-border bg-background", accessibilityLabel=`${totalWins} total wins. ${streakLabel(streak)}`
    - Trophy image: source={require("@/assets/images/trophy.png")}, style={{ width: 64, height: 64 }}, resizeMode="contain", className="mb-4", accessibilityLabel="Winning Streak trophy"
    - Total wins number: className="font-nunito-black text-[64px] text-gold leading-none" — this is the visual centerpiece
    - "total wins" sub-label: className="font-nunito-bold text-sm text-text-secondary mt-1"
    - Streak label line: className="font-nunito-bold text-sm text-text-secondary mt-2", text = streakLabel(streak)
    - No StyleSheet.create — NativeWind className only (exception: style prop for width/height pixel values)
    - Named export (not default): export function HistoryHeroHeader
  </behavior>

  <action>
    Create src/components/HistoryHeroHeader.tsx. Import View, Text, Image from "react-native". Import streakLabel from "@/src/utils/streakLabel". Define HistoryHeroHeaderProps interface. Implement the column-stack layout per the UI-SPEC: outer View with items-center px-4 py-6 border-b border-border bg-background, trophy at 64×64, total wins number in font-nunito-black text-[64px] text-gold leading-none, sub-label in font-nunito-bold text-sm text-text-secondary mt-1, streak line in font-nunito-bold text-sm text-text-secondary mt-2.

    This component must NOT reuse StreakHeader (per D-05) — it is a new component with different layout hierarchy (total wins prominent, streak secondary).

    Run npx tsc --noEmit to confirm no TypeScript errors.
  </action>

  <verify>
    <automated>npx tsc --noEmit && grep -n "export function HistoryHeroHeader" src/components/HistoryHeroHeader.tsx && grep -n "font-nunito-black" src/components/HistoryHeroHeader.tsx && grep -n "text-gold" src/components/HistoryHeroHeader.tsx</automated>
  </verify>

  <acceptance_criteria>
    - File src/components/HistoryHeroHeader.tsx exists
    - Named export HistoryHeroHeader confirmed: grep -n "export function HistoryHeroHeader" src/components/HistoryHeroHeader.tsx
    - Trophy at 64×64: grep -n "width: 64" src/components/HistoryHeroHeader.tsx
    - Gold number: grep -n "font-nunito-black" src/components/HistoryHeroHeader.tsx (must find a line with text-[64px] text-gold on the same or adjacent Text element)
    - streakLabel used: grep -n "streakLabel" src/components/HistoryHeroHeader.tsx
    - No StyleSheet.create: grep -c "StyleSheet.create" src/components/HistoryHeroHeader.tsx returns 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>

  <done>HistoryHeroHeader.tsx exists, TypeScript clean, exports HistoryHeroHeader, renders 64px gold total-wins number and streakLabel line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DateSectionHeader component with animated chevron</name>
  <files>src/components/DateSectionHeader.tsx</files>

  <read_first>
    - .planning/phases/03-win-history/03-PATTERNS.md — "DateSectionHeader.tsx (component, event-driven + Reanimated)" section with full implementation pattern
    - .planning/phases/03-win-history/03-UI-SPEC.md — "DateSectionHeader Component" section (exact classes, Pill Badge spec, Chevron rotation animation table)
    - .planning/phases/03-win-history/03-RESEARCH.md — Pattern 3 (Reanimated chevron rotation), Pitfall 2 (React.memo mandatory), Pitfall 5 (useSharedValue must be in section header not renderItem)
    - src/components/WinCard.tsx — Reanimated import pattern (lines 1-2) and Animated.View usage
    - src/components/WinInputArea.tsx — Pressable + accessibilityRole="button" pattern
  </read_first>

  <behavior>
    - DateSectionHeaderProps: { section: WinSection; isCollapsed: boolean; onToggle: () => void }
    - WinSection interface defined locally: { date_key: string; data: { id: string; [key: string]: unknown }[] }
    - winCountLabel defined at module level (not inside component): (count: number): string => count === 1 ? "1 win" : `${count} wins`
    - Wrapped in React.memo (mandatory per RESEARCH.md Pitfall 2 / RN #43597 sticky header glitch mitigation)
    - Reanimated: useSharedValue(isCollapsed ? 180 : 0), useEffect tracking isCollapsed prop → rotation.value = withTiming(isCollapsed ? 180 : 0, { duration: 200 }), useAnimatedStyle returning transform: [{ rotate: `${rotation.value}deg` }]
    - Layout: Pressable, className="flex-row items-center px-4 py-3 bg-background border-b border-border", accessibilityRole="button", accessibilityLabel=`${dateLabel}, ${winCountLabel(count)}, ${isCollapsed ? "collapsed" : "expanded"}`
    - Date label (left): Text, className="font-nunito-bold text-sm text-text-primary flex-1"
    - Pill badge: View className="bg-gold/20 rounded-full px-2 py-0.5", accessibilityElementsHidden={true}; Text className="font-nunito-bold text-xs text-text-primary"
    - Chevron: Animated.View style={[animatedStyle, { marginLeft: 8 }]}, accessibilityElementsHidden={true}; Ionicons name="chevron-down" size={16} color="#8E8E93"
    - Ionicons color is an imperative prop, never a className
    - No StyleSheet.create — NativeWind className only (exception: style prop for marginLeft pixel value and animatedStyle)
  </behavior>

  <action>
    Create src/components/DateSectionHeader.tsx. Imports: React (for React.memo and useEffect), View, Text, Pressable from "react-native", Animated + useSharedValue + useAnimatedStyle + withTiming from "react-native-reanimated", Ionicons from "@expo/vector-icons", formatDateKey from "@/src/utils/dateUtils".

    Define winCountLabel at module level. Define WinSection interface. Implement DateSectionHeader as React.memo wrapping the inner function. Chevron useSharedValue initializes from isCollapsed prop. useEffect syncs rotation to isCollapsed changes with withTiming duration 200. Pill badge uses bg-gold/20 (fallback: bg-[#FEF8DC] if NativeWind opacity modifier renders incorrectly).

    Run npx tsc --noEmit to confirm no TypeScript errors.
  </action>

  <verify>
    <automated>npx tsc --noEmit && grep -n "React.memo" src/components/DateSectionHeader.tsx && grep -n "useSharedValue" src/components/DateSectionHeader.tsx && grep -n "withTiming" src/components/DateSectionHeader.tsx</automated>
  </verify>

  <acceptance_criteria>
    - File src/components/DateSectionHeader.tsx exists
    - React.memo wrapper: grep -n "React.memo" src/components/DateSectionHeader.tsx returns a line
    - Reanimated imports: grep -n "useSharedValue\|useAnimatedStyle\|withTiming" src/components/DateSectionHeader.tsx returns 3 lines
    - formatDateKey imported: grep -n "formatDateKey" src/components/DateSectionHeader.tsx
    - chevron-down Ionicon: grep -n "chevron-down" src/components/DateSectionHeader.tsx
    - accessibilityElementsHidden on pill and chevron: grep -c "accessibilityElementsHidden" src/components/DateSectionHeader.tsx returns 2
    - winCountLabel singular check: grep -n '"1 win"' src/components/DateSectionHeader.tsx (the "1 win" literal present)
    - No StyleSheet.create: grep -c "StyleSheet.create" src/components/DateSectionHeader.tsx returns 0
    - npx tsc --noEmit exits 0
    - Full test suite still green: npm test
  </acceptance_criteria>

  <done>DateSectionHeader.tsx exists with React.memo, Reanimated chevron, formatDateKey, pill badge, and full accessibility labels. TypeScript clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → component props | totalWins and streak read from Zustand store; win text from local SQLite; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | HistoryHeroHeader, DateSectionHeader | accept | Presentational components rendering local SQLite data. No PII beyond win text (user's own input). No network calls, no external data. Local-only V1 app. |
| T-03-03 | Denial of Service | DateSectionHeader chevron animation | accept | Reanimated animation runs on UI thread; at worst one animation per section header tap. No runaway animation loop possible from this pattern. |
</threat_model>

<verification>
npx tsc --noEmit
npm test
grep -rn "export function HistoryHeroHeader" src/components/HistoryHeroHeader.tsx
grep -rn "React.memo" src/components/DateSectionHeader.tsx
</verification>

<success_criteria>
- HistoryHeroHeader.tsx exports HistoryHeroHeader with 64px gold total-wins number and streakLabel line
- DateSectionHeader.tsx exports React.memo-wrapped DateSectionHeader with Reanimated chevron and pill badge
- Both components use NativeWind className only (no StyleSheet.create)
- npx tsc --noEmit clean
- npm test green (all 30+ existing tests still pass)
</success_criteria>

<output>
After completion, create .planning/phases/03-win-history/03-02-SUMMARY.md
</output>
