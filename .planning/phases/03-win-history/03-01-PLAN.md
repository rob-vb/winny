---
phase: 03-win-history
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/utils/dateUtils.ts
  - src/__tests__/historyUtils.test.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "formatDateKey('today-date-key') returns 'Today'"
    - "formatDateKey('yesterday-date-key') returns 'Yesterday'"
    - "formatDateKey(same-year-other) returns 'EEE, MMM d' format e.g. 'Sat, May 9'"
    - "formatDateKey(prior-year) returns 'MMM d, yyyy' format e.g. 'Dec 1, 2025'"
    - "groupWinsByDate([]) returns []"
    - "groupWinsByDate groups wins by date_key, newest section first"
    - "Within each group wins are sorted by logged_at DESC"
    - "winCountLabel(1) returns '1 win'; winCountLabel(3) returns '3 wins'"
  artifacts:
    - path: "src/utils/dateUtils.ts"
      provides: "formatDateKey export"
      contains: "export function formatDateKey"
    - path: "src/__tests__/historyUtils.test.ts"
      provides: "HIST-01 and HIST-02 unit tests"
      exports: ["formatDateKey suite", "groupWinsByDate suite", "winCountLabel suite"]
  key_links:
    - from: "src/__tests__/historyUtils.test.ts"
      to: "src/utils/dateUtils.ts"
      via: "import { formatDateKey } from '@/src/utils/dateUtils'"
      pattern: "import.*formatDateKey.*dateUtils"
---

## Phase Goal

**As a** user who has logged wins, **I want to** see my complete win history grouped by date with collapsible sections, **so that** I have proof I am already winning.

<objective>
Add the formatDateKey utility to the existing dateUtils.ts file (additive — do not modify existing exports) and create the historyUtils test suite covering the pure functions that the wins screen depends on.

Purpose: Establish the tested utility contract before building the screen. All subsequent plans depend on these pure functions being correct and tested. The formatDateKey function is the critical date-display logic for HIST-01 (relative-then-absolute labels, noon-anchor-safe).

Output:
- src/utils/dateUtils.ts — formatDateKey export added at end of file
- src/__tests__/historyUtils.test.ts — unit tests for formatDateKey, groupWinsByDate, winCountLabel
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-win-history/03-CONTEXT.md
@.planning/phases/03-win-history/03-RESEARCH.md
@.planning/phases/03-win-history/03-PATTERNS.md
@.planning/phases/03-win-history/03-VALIDATION.md
</context>

<interfaces>
From src/utils/dateUtils.ts (existing — read before editing):
```typescript
export function toDateKey(date: Date = new Date()): string
export function computeStreak(distinctDateKeys: string[]): number
// Noon anchor pattern used throughout: new Date(dateKey + "T12:00:00")
```

From src/__tests__/dateUtils.test.ts (test file pattern to mirror):
```typescript
// jest.useFakeTimers() + jest.setSystemTime(new Date("2026-05-10T12:00:00")) in beforeEach
// jest.useRealTimers() in afterEach
// describe("fnName (REQ-ID)", () => { it("behavior description", () => { ... }); });
```

From src/db/schema.ts (Win type for groupWinsByDate):
```typescript
// Win has: id (string UUID), text (string), date_key (string YYYY-MM-DD), logged_at (string ISO)
// The groupWinsByDate function is defined locally in this test file (not imported) — test the logic directly
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Add formatDateKey to dateUtils.ts (RED then GREEN)</name>
  <files>src/utils/dateUtils.ts, src/__tests__/historyUtils.test.ts</files>

  <read_first>
    - src/utils/dateUtils.ts — existing file; formatDateKey must be appended after computeStreak without modifying existing code
    - src/__tests__/dateUtils.test.ts — exact jest.useFakeTimers() pattern to replicate
    - .planning/phases/03-win-history/03-PATTERNS.md — "src/utils/dateUtils.ts (utility, additive)" section and "historyUtils.test.ts structure" section
    - .planning/phases/03-win-history/03-RESEARCH.md — Pattern 4 (formatDateKey implementation) and Pitfall 3 (UTC midnight bug — noon anchor mandatory)
    - .planning/phases/03-win-history/03-UI-SPEC.md — "Date Label Formatting Contract" section
  </read_first>

  <behavior>
    RED phase — write these tests first in src/__tests__/historyUtils.test.ts, verify they fail:

    formatDateKey suite (pin system time to 2026-05-11T12:00:00 via jest.useFakeTimers):
    - formatDateKey("2026-05-11") → "Today"
    - formatDateKey("2026-05-10") → "Yesterday"
    - formatDateKey("2026-05-09") → "Sat, May 9" (EEE, MMM d format)
    - formatDateKey("2025-12-01") → "Dec 1, 2025" (MMM d, yyyy format — prior year)

    winCountLabel suite (pure function, no timer needed):
    - winCountLabel(1) → "1 win"
    - winCountLabel(0) → "0 wins"
    - winCountLabel(3) → "3 wins"
    - winCountLabel(10) → "10 wins"

    groupWinsByDate suite (define the function locally in the test file):
    - groupWinsByDate([]) → []
    - groupWinsByDate with wins on two dates → two sections, newest date_key first
    - groupWinsByDate within a single date group → wins sorted by logged_at DESC (later logged_at appears first)
    - groupWinsByDate with one win → single section with one item

    GREEN phase — implement formatDateKey in src/utils/dateUtils.ts:
    - Append at bottom of dateUtils.ts after computeStreak (additive — never modify toDateKey or computeStreak)
    - Add import: import { isToday, isYesterday, isSameYear, format } from "date-fns" at top of file (or at point of use if tree-shaking preferred)
    - Implementation: noon anchor (dateKey + "T12:00:00"), then isToday → "Today", isYesterday → "Yesterday", isSameYear → format(d, "EEE, MMM d"), else → format(d, "MMM d, yyyy")

    winCountLabel and groupWinsByDate are pure functions defined locally in the test file (not exported from dateUtils.ts). They test the logic that will be inlined in wins.tsx and DateSectionHeader.tsx.
  </behavior>

  <action>
    RED: Create src/__tests__/historyUtils.test.ts with the full test suite described in the behavior block. Use jest.useFakeTimers() / jest.setSystemTime(new Date("2026-05-11T12:00:00")) in beforeEach, jest.useRealTimers() in afterEach. Import formatDateKey from "@/src/utils/dateUtils". Define winCountLabel and groupWinsByDate locally in the test file (these are the same implementations that will appear in the components). Run npm test -- --testPathPattern=historyUtils — tests MUST fail (RED confirmed).

    GREEN: Append formatDateKey to src/utils/dateUtils.ts — add date-fns imports at top of file alongside existing imports, then append the export function formatDateKey at the bottom. Do not alter toDateKey or computeStreak. Run npm test -- --testPathPattern=historyUtils — all tests MUST pass.

    Noon anchor is non-negotiable: always new Date(dateKey + "T12:00:00"). The date-fns functions isToday, isYesterday, isSameYear, format are all already available (date-fns 4.1.0 in node_modules).
  </action>

  <verify>
    <automated>npm test -- --testPathPattern=historyUtils --passWithNoTests</automated>
  </verify>

  <acceptance_criteria>
    - src/__tests__/historyUtils.test.ts exists with describe blocks for formatDateKey, winCountLabel, and groupWinsByDate
    - All tests in historyUtils.test.ts pass (GREEN)
    - src/utils/dateUtils.ts exports formatDateKey — confirmed by: grep -n "export function formatDateKey" src/utils/dateUtils.ts
    - toDateKey and computeStreak are unmodified — confirmed by: npm test -- --testPathPattern=dateUtils
    - formatDateKey uses noon anchor: grep -n "T12:00:00" src/utils/dateUtils.ts returns a line inside formatDateKey
    - winCountLabel(1) test asserts "1 win" (singular), not "1 wins"
    - groupWinsByDate sorts sections newest date_key first (test asserts section[0].date_key is the most recent)
    - npx tsc --noEmit exits 0 (no TypeScript errors)
    - Full test suite still green: npm test
  </acceptance_criteria>

  <done>All historyUtils tests green. formatDateKey exported from dateUtils.ts. Full suite passes. tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| store → component | Zustand wins[] read; data originates from SQLite (local device only, no network) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | dateUtils.formatDateKey | accept | Pure local date formatting; no PII, no network, no user input. Read-only utility. |
</threat_model>

<verification>
npm test -- --testPathPattern=historyUtils
npx tsc --noEmit
npm test
</verification>

<success_criteria>
- src/utils/dateUtils.ts exports formatDateKey with noon anchor
- src/__tests__/historyUtils.test.ts covers formatDateKey (4 cases), winCountLabel (4 cases), groupWinsByDate (4 cases) — all green
- Full test suite green (npm test passes)
- TypeScript clean (npx tsc --noEmit exits 0)
</success_criteria>

<output>
After completion, create .planning/phases/03-win-history/03-01-SUMMARY.md
</output>
