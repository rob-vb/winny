---
phase: 06-onboarding-copy-system
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/copy/catalog.ts
  - src/utils/streakLabel.ts
  - src/notifications/notificationService.ts
  - src/utils/postWinMoment.ts
  - src/__tests__/copyCatalog.test.ts
  - src/__tests__/postWinMoment.test.ts
  - src/__tests__/streakLabel.test.ts
  - src/__tests__/notificationService.test.ts
autonomous: true
requirements:
  - COPY-01
  - COPY-02
must_haves:
  truths:
    - "A central typed copy catalog exports copy for first win, 7/30/100 milestones, comeback, generic post-save, long streak, Home empty, History empty, Dream Goal empty, notification disabled, save/load errors, and notification prompt variants"
    - "All user-facing emotional copy variants avoid banned guilt/shame language"
    - "Variant selection is deterministic for the same seed"
    - "streakLabel(streak) remains a compatibility wrapper and preserves existing threshold behavior"
    - "notificationService pickPromptForDate(dateKey) remains deterministic and returns catalog-backed notification prompt copy"
    - "postWinMoment helpers detect first win, exact 7/30/100 milestones, and comeback gaps using local date keys"
    - "D-13: V1 uses a central typed copy catalog with named emotional states"
    - "D-14: The catalog covers roadmap states plus existing empty/loading/error/disabled states that carry emotional tone"
    - "D-15: Emotional states use small variant arrays, generally 3-5 variants where freshness matters"
    - "D-16: Variant selection is deterministic by date, streak, or win count"
    - "D-17: Existing helpers wrap the catalog for compatibility, including streakLabel and notification prompt copy"
  artifacts:
    - path: "src/copy/catalog.ts"
      provides: "Typed emotional copy states and deterministic selection helpers"
    - path: "src/utils/postWinMoment.ts"
      provides: "Pure first-win, milestone, comeback, and generic post-save moment helpers"
    - path: "src/__tests__/copyCatalog.test.ts"
      provides: "Catalog coverage, deterministic selection, and no-guilt audit tests"
    - path: "src/__tests__/postWinMoment.test.ts"
      provides: "Milestone threshold and comeback date-gap tests"
  key_links:
    - from: "src/utils/streakLabel.ts"
      to: "src/copy/catalog.ts"
      via: "compatibility wrapper around catalog streak copy"
      pattern: "getStreakCopy"
    - from: "src/notifications/notificationService.ts"
      to: "src/copy/catalog.ts"
      via: "notification prompt pool delegates to catalog variants"
      pattern: "notificationPrompts"
---

## Phase Goal

**As a** Winning Streak user, **I want** all emotional messages to feel encouraging and consistent, **so that** streaks, misses, comebacks, and reminders support momentum without shame.

<objective>
Create the central typed copy catalog and pure post-win moment helpers before any UI wiring.

This plan satisfies the core COPY-01/COPY-02 logic contract: catalog coverage, deterministic variants, no-guilt copy audit, streakLabel compatibility, notification prompt compatibility, milestone thresholds, and comeback date-gap detection.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-onboarding-copy-system/06-CONTEXT.md
@.planning/phases/06-onboarding-copy-system/06-RESEARCH.md
@.planning/phases/06-onboarding-copy-system/06-VALIDATION.md
@.planning/phases/06-onboarding-copy-system/06-UI-SPEC.md
@.planning/phases/06-onboarding-copy-system/06-PATTERNS.md
</context>

<interfaces>
From `src/utils/streakLabel.ts`:
```typescript
export function streakLabel(streak: number): string
```

From `src/notifications/notificationService.ts`:
```typescript
export const COPY_POOL: readonly string[]
export function pickPromptForDate(dateKey: string): string
```

New catalog interface:
```typescript
export type CopyState =
  | "firstWin"
  | "milestone7"
  | "milestone30"
  | "milestone100"
  | "comeback"
  | "postSave"
  | "longStreak"
  | "homeEmpty"
  | "historyEmpty"
  | "dreamGoalEmpty"
  | "notificationDisabled"
  | "saveError"
  | "loadError"
  | "notificationPrompt";

export function pickCopyVariant(variants: readonly string[], seed: string | number): string
export function getStreakCopy(streak: number): string
export function getPostWinCopy(moment: PostWinMoment): { title: string; body: string }
```

New post-win interface:
```typescript
export type PostWinMoment =
  | { type: "first-win" }
  | { type: "milestone"; milestone: 7 | 30 | 100 }
  | { type: "comeback" }
  | { type: "post-save" };

export function getStreakMilestone(streak: number): 7 | 30 | 100 | null
export function isComebackWin(previousLatestDateKey: string | null, todayDateKey: string): boolean
export function getPostWinMoment(input: {
  previousTotalWins: number;
  nextStreak: number;
  previousLatestDateKey: string | null;
  todayDateKey: string;
}): PostWinMoment
```
</interfaces>

<tasks>

<task type="tdd">
  <name>Task 1: Write copy catalog and post-win moment tests</name>
  <files>src/__tests__/copyCatalog.test.ts, src/__tests__/postWinMoment.test.ts</files>
  <read_first>
    - src/__tests__/streakLabel.test.ts — existing no-guilt and threshold test style
    - src/__tests__/notificationService.test.ts — deterministic date prompt test style
    - src/__tests__/dateUtils.test.ts — local date-key test style
    - .planning/phases/06-onboarding-copy-system/06-UI-SPEC.md — required copy states and locked banner copy
    - .planning/phases/06-onboarding-copy-system/06-VALIDATION.md — Wave 0 test map
  </read_first>
  <action>
    Create `src/__tests__/copyCatalog.test.ts`. Import the future catalog exports from `@/src/copy/catalog`.

    Include tests that assert:
    - required copy state keys exist for first win, 7/30/100 milestones, comeback, post-save, long streak, Home empty, History empty, Dream Goal empty, notification disabled, save/load errors, and notification prompt variants.
    - every user-facing string in the catalog fails to match `/missed|forgot|failed|failure|broke|broken|lost|punish|shame|guilt|sorry|oops|should have|don't break/i`.
    - `pickCopyVariant(["A","B","C"], "2026-05-13")` returns the same value on repeated calls.
    - `pickCopyVariant` distributes across at least two values over 30 date-key seeds.
    - `getStreakCopy(0)`, `getStreakCopy(1)`, `getStreakCopy(7)`, `getStreakCopy(30)`, and `getStreakCopy(100)` return strings containing the expected count/tier.
    - notification prompt variants are non-empty and include at least five entries.

    Create `src/__tests__/postWinMoment.test.ts`. Import future helpers from `@/src/utils/postWinMoment`.

    Include tests that assert:
    - `getStreakMilestone(7) === 7`, `getStreakMilestone(30) === 30`, `getStreakMilestone(100) === 100`.
    - non-thresholds such as 6, 8, 29, 31, 99, and 101 return null.
    - `isComebackWin(null, "2026-05-13") === false`.
    - previous latest date "2026-05-12" and today "2026-05-13" returns false.
    - previous latest date "2026-05-11" and today "2026-05-13" returns true.
    - `getPostWinMoment` returns first-win when `previousTotalWins` is 0.
    - milestone takes precedence over comeback when `nextStreak` is 7, 30, or 100.
    - comeback returns when prior total wins exist, no milestone is reached, and date gap is greater than one day.
    - otherwise returns post-save.

    Run `npx jest --runInBand src/__tests__/copyCatalog.test.ts src/__tests__/postWinMoment.test.ts`; it must fail before implementation.
  </action>
  <verify>
    <automated>npx jest --runInBand src/__tests__/copyCatalog.test.ts src/__tests__/postWinMoment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/__tests__/copyCatalog.test.ts` exists and imports from `@/src/copy/catalog`.
    - `src/__tests__/postWinMoment.test.ts` exists and imports from `@/src/utils/postWinMoment`.
    - Initial test run fails because implementation files are missing or exports are missing.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 2: Implement copy catalog and post-win moment helpers</name>
  <files>src/copy/catalog.ts, src/utils/postWinMoment.ts</files>
  <read_first>
    - src/__tests__/copyCatalog.test.ts — tests from Task 1
    - src/__tests__/postWinMoment.test.ts — tests from Task 1
    - src/utils/dateUtils.ts — noon-anchor local date-key pattern
    - src/constants/examplePrompts.ts — constant export style
    - .planning/phases/06-onboarding-copy-system/06-UI-SPEC.md — locked copy and state coverage
  </read_first>
  <action>
    Create `src/copy/catalog.ts`. Export typed copy state data plus deterministic helpers.

    Required concrete values:
    - first win title/body: `"First win logged"` / `"That's the whole move. Notice one win, then come back tomorrow."`
    - milestone 7 title/body: `"7 days of wins"` / `"A full week of noticing what is working. Keep building."`
    - milestone 30 title/body: `"30 days strong"` / `"That's a real rhythm. Your wins are starting to stack."`
    - milestone 100 title/body: `"100 days of proof"` / `"You have built something rare. One win at a time."`
    - comeback title/body: `"You're back in motion"` / `"Today counts. Start from this win and keep going."`
    - post-save title/body: `"Win added"` / `"One more piece of proof that you're moving."`
    - include notification prompts from existing `COPY_POOL`: `"What was your win today?"`, `"One small win counts. Log it."`, `"Time to notice what's working."`, `"Your dream is built one win at a time."`, `"Add one win — that's enough."`

    Implement `pickCopyVariant` with a small stable hash over `String(seed)` and modulo variant count. Do not use `Math.random()`.

    Create `src/utils/postWinMoment.ts`. Implement helpers using local `YYYY-MM-DD` date-key strings. For date differences, parse `${dateKey}T12:00:00` to avoid DST boundary bugs, then round day difference by 86400000.

    Run the two new test files until green.
  </action>
  <verify>
    <automated>npx jest --runInBand src/__tests__/copyCatalog.test.ts src/__tests__/postWinMoment.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/copy/catalog.ts` exports all named helpers used by tests.
    - `src/utils/postWinMoment.ts` exports `getStreakMilestone`, `isComebackWin`, and `getPostWinMoment`.
    - New tests pass with `npx jest --runInBand src/__tests__/copyCatalog.test.ts src/__tests__/postWinMoment.test.ts`.
    - No implementation uses `Math.random()` for copy selection.
  </acceptance_criteria>
</task>

<task type="tdd">
  <name>Task 3: Preserve streak and notification compatibility wrappers</name>
  <files>src/utils/streakLabel.ts, src/notifications/notificationService.ts, src/__tests__/streakLabel.test.ts, src/__tests__/notificationService.test.ts</files>
  <read_first>
    - src/utils/streakLabel.ts — current public helper signature and exact threshold strings
    - src/notifications/notificationService.ts — current `COPY_POOL` and `pickPromptForDate`
    - src/__tests__/streakLabel.test.ts — existing threshold expectations
    - src/__tests__/notificationService.test.ts — existing prompt pool expectations
    - src/copy/catalog.ts — new catalog helpers
  </read_first>
  <action>
    Update `src/utils/streakLabel.ts` so `streakLabel(streak)` delegates to `getStreakCopy(streak)` while preserving the existing exported function name and behavior covered by tests.

    Update `src/notifications/notificationService.ts` so `COPY_POOL` is exported from catalog-backed notification variants, and `pickPromptForDate(dateKey)` delegates to `pickCopyVariant(COPY_POOL, dateKey)`. Preserve all other notification service behavior.

    Update tests only if needed to assert catalog delegation while preserving old behavior. Do not weaken existing no-guilt or threshold tests.
  </action>
  <verify>
    <automated>npx jest --runInBand src/__tests__/streakLabel.test.ts src/__tests__/notificationService.test.ts src/__tests__/copyCatalog.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `streakLabel` remains exported from `src/utils/streakLabel.ts`.
    - `COPY_POOL` and `pickPromptForDate` remain exported from `src/notifications/notificationService.ts`.
    - Existing streakLabel and notificationService tests still pass.
    - Copy audit still passes across catalog-backed strings.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Catalog strings -> UI | User-facing copy is imported into app surfaces |
| Date key inputs -> post-win helpers | Local date-key strings drive comeback and milestone feedback |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|----------|-----------------|
| T-06-01 | Tampering | copy catalog | low | Catalog is static source-controlled data; no runtime user input writes catalog strings |
| T-06-02 | Information Disclosure | copy catalog | low | No PII or user data in copy strings |
| T-06-03 | Spoofing | postWinMoment date inputs | low | Helpers operate on persisted local wins; tests cover date-gap boundaries; no external actor |

No high-severity threats. This plan is pure local logic and static copy.
</threat_model>

<verification>
npx jest --runInBand src/__tests__/copyCatalog.test.ts src/__tests__/postWinMoment.test.ts
npx jest --runInBand src/__tests__/streakLabel.test.ts src/__tests__/notificationService.test.ts src/__tests__/copyCatalog.test.ts
</verification>

<success_criteria>
- Copy catalog covers all Phase 6 required emotional states.
- No user-facing emotional copy contains banned guilt/shame terms.
- Streak and notification helpers remain backward-compatible.
- Post-win moment helpers correctly detect first win, 7/30/100 milestones, comeback gaps, and generic post-save state.
</success_criteria>

<decision_coverage>
- D-13: Central typed copy catalog implemented by `src/copy/catalog.ts`.
- D-14: Catalog covers roadmap states plus empty/loading/error/disabled emotional states.
- D-15: Catalog uses small variant arrays where freshness matters.
- D-16: Variant selection is deterministic by date, streak, win count, or seed.
- D-17: `streakLabel()` and notification prompt copy remain compatibility wrappers around catalog copy.
</decision_coverage>

<output>
After completion, create `.planning/phases/06-onboarding-copy-system/06-01-SUMMARY.md`.
</output>
