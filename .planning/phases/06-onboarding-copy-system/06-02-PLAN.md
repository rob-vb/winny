---
phase: 06-onboarding-copy-system
plan: "02"
type: execute
wave: 2
depends_on:
  - "06-01"
files_modified:
  - src/db/repositories/onboarding.ts
  - app/_layout.tsx
  - app/onboarding/welcome.tsx
  - app/onboarding/dream-goal.tsx
  - src/__tests__/onboardingSettings.test.ts
autonomous: true
requirements:
  - ONBD-01
  - ONBD-02
must_haves:
  truths:
    - "Root layout does not render the tab shell while onboarding status is unknown"
    - "Fresh install routes to Welcome before tabs"
    - "Welcome CTA text is Start Winning and routes to onboarding Dream Goal setup with router.replace"
    - "Dream Goal setup has Save Goal disabled until validateGoalText(currentText) is true"
    - "Skip for now writes onboarding_completed=true and does not save an empty Dream Goal"
    - "Save Goal writes the trimmed Dream Goal, writes onboarding_completed=true, shows success copy, then routes Home"
    - "Onboarding never requests notification permission"
    - "D-01: Fresh install uses a light gate before the app: Welcome -> skippable Dream Goal setup -> Home, with tabs hidden until completion"
    - "D-02: Onboarding completes through Save Goal or Skip, then routes Home"
    - "D-03: Once onboarding_completed=true, the first-run gate never shows again; abandoning before completion can resume onboarding"
    - "D-04: Store onboarding_completed=true in the existing SQLite settings key/value table"
    - "D-05: Welcome uses celebratory trophy-led energy"
    - "D-06: Welcome explanation is lightweight: a headline plus one brief method-framing sentence"
    - "D-07: Reuse the existing trophy asset; do not add rocket or astronaut art"
    - "D-08: Welcome primary button text is Start Winning"
    - "D-09: Dream Goal setup encourages entry while keeping Skip for now visible and calm"
    - "D-10: Onboarding uses a simplified Dream Goal editor and reuses repository/validation logic"
    - "D-11: Save action is disabled until valid text exists; Skip is the explicit continue-without-goal path"
    - "D-12: Saving a Dream Goal shows a brief success state, then auto-routes Home"
  artifacts:
    - path: "src/db/repositories/onboarding.ts"
      provides: "hasCompletedOnboarding and completeOnboarding wrappers over settings"
    - path: "app/onboarding/welcome.tsx"
      provides: "First-run Welcome screen"
    - path: "app/onboarding/dream-goal.tsx"
      provides: "Skippable Dream Goal onboarding setup"
    - path: "app/_layout.tsx"
      provides: "Onboarding route gate before tabs"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/db/repositories/onboarding.ts"
      via: "read onboarding status after fonts/migrations are ready"
      pattern: "hasCompletedOnboarding"
    - from: "app/onboarding/dream-goal.tsx"
      to: "src/db/repositories/dreamGoal.ts"
      via: "Save Goal calls upsertGoal(currentText.trim())"
      pattern: "upsertGoal"
    - from: "app/onboarding/dream-goal.tsx"
      to: "src/db/repositories/onboarding.ts"
      via: "Save and Skip both call completeOnboarding()"
      pattern: "completeOnboarding"
---

## Phase Goal

**As a** first-time user, **I want** a short warm onboarding path before the app tabs, **so that** I understand the habit quickly and can start logging a win without friction.

<objective>
Implement the first-run onboarding gate and two onboarding screens: Welcome and skippable Dream Goal setup.

This plan delivers ONBD-01 and ONBD-02: fresh install sees Welcome -> Dream Goal setup -> Home, with tabs hidden until completion and a clear skip path.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-onboarding-copy-system/06-CONTEXT.md
@.planning/phases/06-onboarding-copy-system/06-RESEARCH.md
@.planning/phases/06-onboarding-copy-system/06-UI-SPEC.md
@.planning/phases/06-onboarding-copy-system/06-PATTERNS.md
</context>

<interfaces>
From `src/db/repositories/settings.ts`:
```typescript
export async function getSetting(key: string): Promise<string | null>
export async function setSetting(key: string, value: string): Promise<void>
```

From `src/db/repositories/dreamGoal.ts`:
```typescript
export async function upsertGoal(text: string): Promise<void>
```

From `src/utils/goalValidation.ts`:
```typescript
export function validateGoalText(text: string): boolean
```

New onboarding repository:
```typescript
export async function hasCompletedOnboarding(): Promise<boolean>
export async function completeOnboarding(): Promise<void>
```
</interfaces>

<tasks>

<task type="execute">
  <name>Task 1: Add onboarding settings wrapper and tests</name>
  <files>src/db/repositories/onboarding.ts, src/__tests__/onboardingSettings.test.ts</files>
  <read_first>
    - src/db/repositories/settings.ts — current getSetting/setSetting signatures
    - src/__tests__/notificationService.test.ts — module mock style for settings-related behavior
    - .planning/phases/06-onboarding-copy-system/06-VALIDATION.md — onboarding settings test requirement
  </read_first>
  <action>
    Create `src/db/repositories/onboarding.ts` with two exports:
    - `hasCompletedOnboarding()` reads `getSetting("onboarding_completed")` and returns true only when the value is exactly `"true"`.
    - `completeOnboarding()` writes `setSetting("onboarding_completed", "true")`.

    Create `src/__tests__/onboardingSettings.test.ts`. Mock `@/src/db/repositories/settings` and assert:
    - `hasCompletedOnboarding()` returns false for null.
    - `hasCompletedOnboarding()` returns false for `"false"`.
    - `hasCompletedOnboarding()` returns true for `"true"`.
    - `completeOnboarding()` calls `setSetting("onboarding_completed", "true")`.
  </action>
  <verify>
    <automated>npx jest --runInBand src/__tests__/onboardingSettings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/db/repositories/onboarding.ts` exports both helpers.
    - Test file passes with `npx jest --runInBand src/__tests__/onboardingSettings.test.ts`.
    - No schema or migration file is changed.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 2: Gate root layout before tabs</name>
  <files>app/_layout.tsx</files>
  <read_first>
    - app/_layout.tsx — current font, migration, splash, AppState notification top-up logic
    - src/db/repositories/onboarding.ts — helper from Task 1
    - .planning/phases/06-onboarding-copy-system/06-RESEARCH.md — Pitfall 1 and Pitfall 2
    - .planning/phases/06-onboarding-copy-system/06-UI-SPEC.md — Root Onboarding Gate state machine
  </read_first>
  <action>
    Update `app/_layout.tsx` to add onboarding status readiness after the existing fonts/migrations readiness.

    Concrete target:
    - import `useState` from React in addition to existing hooks.
    - import `useRouter` and `useSegments` from `expo-router` if needed for route replacement.
    - import `hasCompletedOnboarding` from `@/src/db/repositories/onboarding`.
    - add local state: `const [onboardingChecked, setOnboardingChecked] = useState(false); const [onboardingComplete, setOnboardingComplete] = useState(false);`
    - after `ready` becomes true, call `hasCompletedOnboarding()` in an effect, set both state fields, and do not read before `ready`.
    - while `!ready || !onboardingChecked`, return null so tabs do not flash.
    - when `ready && onboardingChecked && !onboardingComplete` and the current route is not already under `onboarding`, call `router.replace("/onboarding/welcome")`.
    - include Stack entries for `onboarding/welcome` and `onboarding/dream-goal` with `headerShown: false`.
    - preserve existing notification AppState top-up behavior and `settings/how-it-works` Stack entry.

    Do not request notification permission or schedule notifications from onboarding code.
  </action>
  <verify>
    <automated>npx jest --runInBand src/__tests__/onboardingSettings.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `app/_layout.tsx` contains `hasCompletedOnboarding`.
    - `app/_layout.tsx` does not render `<Stack>` until onboarding status has been checked.
    - Stack includes `onboarding/welcome` and `onboarding/dream-goal` routes with hidden headers.
    - Existing imports and AppState notification code still compile.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 3: Build Welcome screen</name>
  <files>app/onboarding/welcome.tsx</files>
  <read_first>
    - app/(tabs)/index.tsx — trophy image import and Home empty-state layout
    - src/components/WinInputArea.tsx — orange CTA class pattern
    - .planning/phases/06-onboarding-copy-system/06-UI-SPEC.md — Welcome screen layout and locked copy
  </read_first>
  <action>
    Create `app/onboarding/welcome.tsx`.

    Implement a React Native screen with:
    - `SafeAreaView className="flex-1 bg-background"`
    - centered content `View className="flex-1 px-6 items-center justify-center"`
    - `Image` using `require("@/assets/images/trophy.png")`, `style={{ width: 132, height: 132 }}`, `resizeMode="contain"`, `accessibilityLabel="Winning Streak trophy"`
    - headline text exactly `"Welcome to Winning Streak"` with `font-nunito-bold text-[28px] text-text-primary text-center leading-tight`
    - support text exactly `"Log one small win at a time and build proof that you're already moving."`
    - `Pressable` primary CTA with text exactly `"Start Winning"`, `bg-primary rounded-lg min-h-[44px] px-6 mt-8`
    - onPress calls `router.replace("/onboarding/dream-goal")`
  </action>
  <verify>
    <automated>rtk rg "Welcome to Winning Streak|Start Winning|router.replace\\(\"/onboarding/dream-goal\"\\)" app/onboarding/welcome.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Welcome screen file exists.
    - It uses trophy asset, locked headline, locked support copy, and locked CTA copy.
    - It has no tabs, no skip button, and no notification permission call.
  </acceptance_criteria>
</task>

<task type="execute">
  <name>Task 4: Build skippable onboarding Dream Goal screen</name>
  <files>app/onboarding/dream-goal.tsx</files>
  <read_first>
    - app/(tabs)/goal.tsx — current Dream Goal save/load state pattern
    - src/components/GoalEditor.tsx — TextInput, counter, Save Goal visual pattern
    - src/utils/goalValidation.ts — validateGoalText
    - src/db/repositories/dreamGoal.ts — upsertGoal
    - src/db/repositories/onboarding.ts — completeOnboarding
    - .planning/phases/06-onboarding-copy-system/06-UI-SPEC.md — Onboarding Dream Goal screen contract
  </read_first>
  <action>
    Create `app/onboarding/dream-goal.tsx`.

    Implement local state: `currentText`, `screenState` as `"editing" | "saving" | "saved" | "skipping" | "error"`.

    Render:
    - headline exactly `"Set a Dream Goal"`
    - support exactly `"Optional, but powerful: name what your wins are building toward."`
    - multiline TextInput with placeholder exactly `"What are you working toward?"`, maxLength 500, minHeight 120
    - character counter when <=100 characters remain, same `{remaining} / 500` display used by GoalEditor
    - primary button text exactly `"Save Goal"`, disabled unless `validateGoalText(currentText)` and not saving/skipping
    - secondary button text exactly `"Skip for now"`, transparent, text-secondary
    - success copy exactly `"Saved. Let's log your first win."`
    - save error exactly `"Couldn't save your goal — tap Save Goal to try again."`

    Save handler:
    - if invalid or already saving, return.
    - set saving state.
    - call `upsertGoal(currentText.trim())`.
    - call `completeOnboarding()`.
    - set saved state and show success copy briefly with `setTimeout`.
    - call `router.replace("/(tabs)")` after the brief success state.

    Skip handler:
    - set skipping state.
    - call `completeOnboarding()`.
    - call `router.replace("/(tabs)")`.
    - never call `upsertGoal` on skip.

    Failure behavior:
    - Save failure keeps `currentText` intact and sets error state.
    - Skip failure can also set error state with the same practical retry copy.
  </action>
  <verify>
    <automated>rtk rg "Set a Dream Goal|Skip for now|completeOnboarding|upsertGoal|Saved\\. Let's log your first win" app/onboarding/dream-goal.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Dream Goal onboarding file exists and imports `validateGoalText`, `upsertGoal`, and `completeOnboarding`.
    - Save path trims and saves non-empty valid text.
    - Skip path completes onboarding without writing an empty Dream Goal.
    - No notification permission or scheduling function is imported.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User text -> Dream Goal persistence | User-entered Dream Goal is saved locally |
| Settings flag -> navigation gate | Local settings value decides first-run route |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Mitigation Plan |
|-----------|----------|-----------|----------|-----------------|
| T-06-04 | Tampering | onboarding_completed setting | low | Local-only preference; completion writes exact string `"true"` through repository wrapper |
| T-06-05 | Denial of Service | onboarding gate | medium | Root layout must render null only until status is checked; manual fresh-install test verifies no routing loop |
| T-06-06 | Information Disclosure | Dream Goal setup | low | Goal text remains in local SQLite; no network calls or notification prompt in onboarding |

No high-severity threats. Main risk is a local routing loop, mitigated by explicit route checks and manual verification.
</threat_model>

<verification>
npx jest --runInBand src/__tests__/onboardingSettings.test.ts
rtk rg "Welcome to Winning Streak|Start Winning" app/onboarding/welcome.tsx
rtk rg "Set a Dream Goal|Skip for now|completeOnboarding|upsertGoal" app/onboarding/dream-goal.tsx
</verification>

<success_criteria>
- Fresh install routes Welcome -> Dream Goal setup -> Home, with tabs hidden before completion.
- Dream Goal setup can be skipped and later completed from the Dream Goal tab.
- Saving Dream Goal during onboarding persists the goal and completes onboarding.
- Onboarding never changes notification permission timing.
</success_criteria>

<decision_coverage>
- D-01: Fresh install uses the light gate before tabs: Welcome -> skippable Dream Goal setup -> Home.
- D-02: Onboarding completes through Save Goal or Skip, then routes Home.
- D-03: `onboarding_completed=true` prevents the first-run gate from showing again; abandoning before completion can resume onboarding.
- D-04: `onboarding_completed=true` is stored in the existing SQLite `settings` key/value table.
- D-05: Welcome uses trophy-led celebratory energy with the existing trophy asset.
- D-06: Welcome explanation stays lightweight: one headline plus one brief method-framing sentence.
- D-07: Existing `assets/images/trophy.png` is reused; no new rocket/astronaut asset is created.
- D-08: Welcome primary button text is exactly `Start Winning`.
- D-09: Dream Goal setup encourages entry while keeping `Skip for now` visible and calm.
- D-10: Onboarding uses a simplified Dream Goal editor that reuses repository and validation logic.
- D-11: Save Goal is disabled until valid text exists; Skip is the explicit continue-without-goal path.
- D-12: Saving a Dream Goal shows a brief success state, then routes Home.
</decision_coverage>

<output>
After completion, create `.planning/phases/06-onboarding-copy-system/06-02-SUMMARY.md`.
</output>
